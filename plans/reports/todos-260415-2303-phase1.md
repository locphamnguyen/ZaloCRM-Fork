# Phase 1 TODOs

**Date:** 2026-04-15
**Source:** Eng Review

---

## TODO-1: Rate-limit + backoff cho polling sync
**Priority:** Critical
**Phase:** 1 (BUG-01)
**Depends on:** TODO-5 (research zca-js)

Khi polling sync gọi API zca-js, cần cơ chế retry với exponential backoff khi bị rate-limit. Thêm log + notification khi sync bị chặn. Ngăn ngừa silent failure — đây là critical gap duy nhất trong review.

---

## TODO-2: Composite index cho Conversation filters
**Priority:** High
**Phase:** 1 (FEATURE-02)

Thêm `@@index([orgId, zaloAccountId, isReplied, lastMessageAt])` vào Conversation model trong Prisma schema. Hiện chỉ có unique index trên `[zaloAccountId, externalThreadId]`. Không có index = slow query khi nhiều conversations.

---

## TODO-3: Runtime validation cho AutomationRule JSON
**Priority:** Low
**Phase:** 1 (code quality)

Thêm guard check trong `automation-service.ts:51` trước khi cast `rule.conditions` và `rule.actions` từ JSON. Hiện cast trực tiếp `as unknown as AutomationCondition[]` không validate. Quan trọng hơn khi mở API cho bên ngoài (đợt 2-3).

---

## TODO-4: Thống nhất cú pháp biến template
**Priority:** Medium
**Phase:** 1 (FEATURE-07)

Feature request dùng `{crm_name}` (ngoặc đơn), code hiện tại dùng `{{ contact.fullName }}` (ngoặc kép). Chọn 1 format duy nhất. Recommend `{{ }}` vì ít va chạm với text thường. Update docs và UI placeholder.

---

## TODO-5: Research zca-js + n8n-nodes-ultimate
**Priority:** Critical (blocker)
**Phase:** 1 (BUG-01, BUG-02)
**Blocks:** TODO-1, BUG-01 implementation, BUG-02 implementation

Đọc tài liệu zca-js và https://codedao12.gitbook.io/n8n-nodes-ultimate/tong-quan/installation để hiểu đầy đủ:
- API sync tin nhắn (getRecentMessages, getConversation, etc.)
- Rate limits per account
- Các loại tin nhắn đặc biệt (QR, bank transfer, missed call, system)
- Cơ chế tin nhắn Zalo cá nhân, group, OA

Phải làm trước khi implement BUG-01 và BUG-02.
